# zkSTB 🍪

```
react-cookie-consent-banner/
├── node_modules/           # Dependencies installed by npm
├── public/                 # Static files for the demo app
│   ├── index.html          # HTML template for the demo app
│   └── manifest.json       # Web app manifest for the demo app
├── src/                    # Source code for the package
│   ├── CookieConsentBanner.tsx     # React component for the cookie consent banner
│   ├── cookieUtils.ts             # Utility functions for working with cookies
│   ├── messageSigningTracker.ts   # Function for signing messages and tracking events
│   └── index.ts                   # Entry point for the package
├── types/                  # TypeScript declaration files for the package
│   ├── cookieUtils.d.ts            # Declarations for cookieUtils.ts
│   ├── messageSigningTracker.d.ts  # Declarations for messageSigningTracker.ts
│   └── index.d.ts                  # Declarations for index.ts
├── .gitignore              # Files and directories ignored by Git
├── package.json            # Metadata and dependencies for the package
├── package-lock.json       # Version lockfile generated by npm
├── README.md               # Documentation for the package
└── tsconfig.json           # TypeScript configuration for the package
```
